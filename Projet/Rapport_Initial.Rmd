---
title: "Projet Gestion de l'Eau --- Rapport reproductible"
subtitle: "Caractérisation, drainage et optimisation d'assolement"
author: "Étudiant 1 (Pédologue-Géostatisticien) -- Étudiant 2 (Hydrodynamicien) -- Étudiant 3 (Agronome-Environnementaliste)"
date: "`r format(Sys.Date(), '%d %B %Y')`"
output:
  html_document:
    theme: cosmo
    toc: true
    toc_depth: 3
    toc_float: true
    number_sections: true
    fig_caption: true
    code_folding: show
    df_print: paged
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(
  echo      = TRUE,
  warning   = FALSE,
  message   = FALSE,
  fig.align = "center",
  fig.width = 8,
  fig.height = 5,
  dpi       = 110
)
options(scipen = 999, digits = 4)
```

# Comment utiliser ce squelette

Ce fichier `Rapport_Initial.Rmd` est un **squelette reproductible** : il contient
des chunks préremplis pour les 5 parties du projet. Vous devez :

1. Compléter les chunks marqués `### À COMPLÉTER ###`.
2. Exécuter le fichier de proche en proche (`Knit` ou Ctrl+Shift+K).
3. Récupérer les tableaux et figures produits pour les **insérer dans le rapport
   PDF final** (cf. `Consignes_Rapport_Gestion_Eau.pdf`).
4. Joindre le `.Rmd` finalisé dans le dossier `R/` de l'archive de remise.

**Le RMarkdown ne remplace pas le rapport** : il en garantit la reproductibilité
et fournit la matière (chiffres, figures, tableaux).

---

# Paquets et données

```{r packages}
# Installer si nécessaire :
# install.packages(c("readxl","minpack.lm","ggplot2","dplyr","tidyr",
#                    "zoo","gstat","sp","sf","knitr","kableExtra"))

library(readxl)
library(minpack.lm)
library(ggplot2)
library(dplyr)
library(tidyr)
library(zoo)
library(gstat)
library(sp)
library(knitr)
library(kableExtra)
```

```{r donnees}
fichier <- "Donnees_Projet_GE.xlsx"

ret    <- read_excel(fichier, sheet = "01_Retention_brute")
ksat   <- read_excel(fichier, sheet = "02_Ksat_spatial")
clim   <- read_excel(fichier, sheet = "03_Climat")
cult   <- read_excel(fichier, sheet = "04_Cultures")
tensio <- read_excel(fichier, sheet = "05_Tensio_saison_prec")
drain  <- read_excel(fichier, sheet = "06_Parametres_drainage")

# Paramètres géotechniques (récupérés depuis l'Excel)
z_drain <- drain$Valeur[drain$Parametre == "z_drain_m"]      # 1.0
D_imp   <- drain$Valeur[drain$Parametre == "D_m"]            # 2.0
u_drain <- drain$Valeur[drain$Parametre == "u_m"]            # 0.2
WT_str  <- drain$Valeur[drain$Parametre == "WT_strict_m"]    # 0.7
WT_tol  <- drain$Valeur[drain$Parametre == "WT_tolerant_m"]  # 0.5
alpha_v <- drain$Valeur[drain$Parametre == "alpha_vanBeers"] # 1.15

cat("Paramètres lus :\n")
cat(sprintf("  z_drain = %.1f m  ;  D = %.1f m  ;  u = %.2f m\n",
            z_drain, D_imp, u_drain))
cat(sprintf("  WT_strict = %.1f m  ;  WT_tolerant = %.1f m\n", WT_str, WT_tol))
```

```{r surfaces}
# Surfaces par zone (cf. énoncé)
surfaces <- tibble(
  Zone       = c("Zone_A", "Zone_B", "Zone_C"),
  Surface_ha = c(1.5,      6.0,      2.5)
)
kable(surfaces, caption = "Surfaces des trois zones (10 ha au total)")
```

---

# PARTIE I --- Caractérisation hydrodynamique des sols (25 pts)

## I.1 -- Ajustement van Genuchten par zone

> **### À COMPLÉTER ###**
>
> Vous devez ajuster le modèle de **van Genuchten (1980)** à chaque zone (A, B, C)
> à partir des données de la feuille `01_Retention_brute`.
>
> Étapes attendues :
>
> 1. Écrire la fonction `vg(h, theta_r, theta_s, alpha, n)` avec la
>    contrainte `m = 1 - 1/n`.
> 2. Utiliser `nlsLM` (package `minpack.lm`) pour ajuster les **4 paramètres**
>    sur les couples `(h_cm, theta_Zone_*)`.
> 3. Choisir et justifier les **valeurs initiales** et les **bornes** des
>    paramètres (cohérence physique : `theta_r < theta_s`, `n > 1`, etc.).
> 4. Construire un tableau `parametres` avec, pour chaque zone :
>    `theta_r, theta_s, alpha, n, m, RMSE, R²` et les **erreurs-types**.
> 5. Discuter la qualité d'ajustement (résidus, R², RMSE) dans le rapport.

```{r vg-fit}
zones <- c("Zone_A", "Zone_B", "Zone_C")

# parametres  <- data.frame()   # à construire (cf. consignes ci-dessus)
# predictions <- data.frame()   # prédictions lissées pour le graphique
# fits        <- list()         # objets nls pour l'annexe A

### À COMPLÉTER : écrire la boucle d'ajustement et remplir `parametres` ###
```

## I.2 -- Courbes de rétention ajustées

> **### À COMPLÉTER ###**
>
> Une fois `parametres` et `predictions` calculés en I.1, tracer sur un même
> graphique (échelle log en abscisse) :
>
> - les **mesures** (points) pour chaque zone,
> - les **courbes ajustées** (lignes continues) issues du modèle VG.
>
> Soigner la légende, les unités et le titre. Commenter les différences entre
> zones (texture, pente à l'inflexion, valeur de `theta_s`).

```{r vg-plot, eval=FALSE, fig.cap="Courbes de rétention ajustées (lignes) et mesures (points)"}
### À COMPLÉTER : ggplot des mesures + courbes VG ajustées ###
```

## I.3 -- Indicateurs dérivés : capacité au champ, PFP, RU

> **### À COMPLÉTER ###**
>
> À partir des paramètres VG ajustés en I.1, calculer pour chaque zone :
>
> - **theta_CC** : teneur en eau à pF 2,5 (succion = 316 cm).
> - **theta_PFP** : teneur en eau à pF 4,2 (succion = 15 849 cm).
> - **RU** sur 1 m de sol (en mm) = `(theta_CC - theta_PFP) * 1000`.
>
> Présenter le résultat sous forme de tableau et commenter les écarts entre
> zones (la RU conditionne directement la fréquence d'irrigation).

```{r indicateurs-sol, eval=FALSE}
### À COMPLÉTER : calcul de theta_CC, theta_PFP et RU à partir de `parametres` ###
```

## I.4 -- Statistiques descriptives de Ksat par zone

```{r ksat-stats}
ksat_par_zone <- ksat %>%
  group_by(Zone) %>%
  summarise(
    n        = n(),
    Ksat_moy = round(mean(Ksat_cm_par_jour), 2),
    Ksat_med = round(median(Ksat_cm_par_jour), 2),
    Ksat_geom = round(exp(mean(log(Ksat_cm_par_jour))), 2),
    Ksat_sd  = round(sd(Ksat_cm_par_jour), 2),
    CV_pct   = round(100 * sd(Ksat_cm_par_jour) / mean(Ksat_cm_par_jour), 1)
  )

kable(ksat_par_zone,
      caption = "Statistiques descriptives de Ksat (cm/jour) par zone") %>%
  kable_styling(full_width = FALSE)
```

## I.5 -- Krigeage ordinaire de ln(Ksat)

> **### À COMPLÉTER ###**
>
> Réaliser le **krigeage ordinaire** de `ln(Ksat)` sur l'ensemble de la parcelle
> (champ 400 m × 250 m, maille 5 m). Étapes attendues :
>
> 1. **Transformation logarithmique** : justifier le recours à `ln(Ksat)`
>    (distribution log-normale, cf. histogramme de I.4).
> 2. **Variogramme expérimental** : calculer avec `gstat::variogram` en
>    choisissant `cutoff` et `width` adaptés à la disposition des points.
> 3. **Ajustement** d'un modèle théorique (sphérique, exponentiel, gaussien) —
>    **comparer au moins deux modèles** et justifier le choix
>    (pépite, palier, portée).
> 4. **Krigeage** sur la grille avec `gstat::krige`.
> 5. **Retour à l'échelle naturelle** : utiliser la correction log-normale
>    `Ksat = exp(prediction + 0,5 × variance)`.
> 6. **Cartes** : produire deux cartes (Ksat prédit, variance de krigeage) et
>    interpréter les zones de forte incertitude.
> 7. **Validation croisée LOO** (`krige.cv`) : biais et RMSE.
>
> Le krigeage n'est pas réutilisé tel quel pour Hooghoudt (qui utilise la moyenne
> géométrique par zone, cf. I.6) ; il sert à **caractériser la structure spatiale**
> et à **discuter de l'hétérogénéité** des sols.

```{r krigeage, eval=FALSE}
### À COMPLÉTER : variogramme expérimental + ajustement d'un modèle ###
```

```{r krigeage-carte, eval=FALSE, fig.cap="Carte krigée de Ksat (cm/j) et carte de la variance de krigeage"}
### À COMPLÉTER : krigeage sur la grille + cartes Ksat et variance ###
```

```{r krigeage-validation, eval=FALSE}
### À COMPLÉTER : validation croisée leave-one-out (biais, RMSE) ###
```

## I.6 -- Moyennes géométriques de Ksat par zone (entrée pour Hooghoudt)

```{r ksat-geom-zone}
# Pour Hooghoudt on utilise la moyenne géométrique
Ksat_geom <- ksat %>%
  group_by(Zone) %>%
  summarise(Ks_cm_j = round(exp(mean(log(Ksat_cm_par_jour))), 1)) %>%
  left_join(surfaces, by = "Zone")

kable(Ksat_geom,
      caption = "Ksat moyen géométrique par zone (entrée Hooghoudt)") %>%
  kable_styling(full_width = FALSE)
```

## I.7 -- Identification de la texture par zone

> **### À COMPLÉTER ###** (nécessite les paramètres VG de I.1 et Ksat_geom de I.6)
>
> Croiser les valeurs ajustées de `theta_s` et `n` (Partie I.1) avec la moyenne
> géométrique de Ksat (I.6) pour proposer une texture USDA probable par zone.
> Justifier en confrontant au triangle USDA et au comportement des tensiomètres
> (feuille `05_Tensio_saison_prec`).

```{r texture, eval=FALSE}
texture_tbl <- parametres %>%
  select(Zone, theta_s, n) %>%
  left_join(Ksat_geom %>% select(Zone, Ks_cm_j), by = "Zone") %>%
  mutate(
    Texture_proba = case_when(
      Ks_cm_j > 80   & n > 1.6 ~ "Sable / sable loameux",
      Ks_cm_j > 15   & n > 1.3 ~ "Loam / loam sableux",
      Ks_cm_j <= 15            ~ "Argile / argile limoneuse",
      TRUE                     ~ "À discuter"
    )
  )

kable(texture_tbl,
      caption = "Texture probable (USDA) à partir de theta_s, n et Ksat") %>%
  kable_styling(full_width = FALSE)
```

> **À justifier dans le rapport :** croiser ces résultats avec le triangle USDA et
> les comportements observés sur les tensiomètres (feuille `05`) pendant la saison
> N-1.

---

# PARTIE II --- Dimensionnement du drainage par zone (15 pts)

## II.1 -- Coefficient de drainage de pointe q

```{r q-coefficient}
# Pluie maximale glissante sur 5 jours
P     <- clim$Pluie_mm
Pmax5 <- max(zoo::rollsum(P, k = 5, fill = 0, align = "right"))
cat(sprintf("Pluie maximale sur 5 jours : %.1f mm\n", Pmax5))

q_mmj <- Pmax5 / 5
q_mj  <- q_mmj / 1000
cat(sprintf("Coefficient retenu : q = %.2f mm/j = %.5f m/j\n", q_mmj, q_mj))

# Comparaison à d'autres critères
Pmax1 <- max(P)
Pmax3 <- max(zoo::rollsum(P, k = 3, fill = 0, align = "right"))

tibble(
  Critere = c("P_max,1j / 3", "P_max,3j / 3", "P_max,5j / 5"),
  q_mm_j  = round(c(Pmax1 / 3, Pmax3 / 3, Pmax5 / 5), 2)
) %>%
  kable(caption = "Comparaison de critères pour le coefficient q") %>%
  kable_styling(full_width = FALSE)
```

## II.2 -- Formules de Hooghoudt et van Beers

```{r hooghoudt-fun}
hooghoudt_L <- function(K_mj, h, d, q) {
  sqrt(4 * K_mj * h * (2 * d + h) / q)
}

hooghoudt_d <- function(L, D, u = 0.2, alpha = 1.15) {
  D / (1 + (D / L) * (8 / pi * log(D / u) - alpha))
}
```

## II.3 -- Calcul itératif de L par zone et par critère

```{r hooghoudt-iter}
# Critères de profondeur de nappe (cf. énoncé)
criteres <- tibble(
  Critere  = c("Strict (Fraise/PdT)", "Tolerant (Tomate/Mais)"),
  WT_cible = c(WT_str, WT_tol),
  h        = z_drain - c(WT_str, WT_tol)
)

res <- expand.grid(Zone    = Ksat_geom$Zone,
                   Critere = criteres$Critere,
                   stringsAsFactors = FALSE) %>%
  left_join(Ksat_geom, by = "Zone") %>%
  left_join(criteres,  by = "Critere") %>%
  mutate(K_mj   = Ks_cm_j / 100,
         L_iter = hooghoudt_L(K_mj, h, D_imp, q_mj))

# 5 itérations sur d
for (i in 1:5) {
  res$d_eq   <- hooghoudt_d(res$L_iter, D_imp, u_drain, alpha_v)
  res$L_iter <- hooghoudt_L(res$K_mj, res$h, res$d_eq, q_mj)
}

res <- res %>%
  mutate(L_iter            = round(L_iter, 1),
         d_eq              = round(d_eq, 2),
         lineaire_m_par_ha = round(10000 / L_iter, 0),
         drains_par_zone   = ceiling(Surface_ha * lineaire_m_par_ha))

kable(res %>% select(Zone, Critere, Ks_cm_j, h, d_eq, L_iter,
                     lineaire_m_par_ha, drains_par_zone),
      caption = "Espacement L*, profondeur équivalente d et linéaire de drains") %>%
  kable_styling(full_width = FALSE)
```

## II.4 -- Sensibilité à K et à q

```{r sensibilite-Kq}
sens <- expand.grid(Zone    = Ksat_geom$Zone,
                    facteur = c(0.7, 1.0, 1.3)) %>%
  left_join(Ksat_geom, by = "Zone") %>%
  mutate(K_mj = (Ks_cm_j / 100) * facteur,
         L_K  = round(hooghoudt_L(K_mj, h = 0.5, d = D_imp, q = q_mj), 1)) %>%
  select(Zone, facteur, L_K) %>%
  pivot_wider(names_from = facteur, values_from = L_K,
              names_prefix = "K_x")

kable(sens,
      caption = "Sensibilité de L (m) à K (+/- 30 %), critère tolérant h=0,5 m") %>%
  kable_styling(full_width = FALSE)

sens_q <- expand.grid(Zone   = Ksat_geom$Zone,
                      q_fact = c(0.5, 1.0, 1.5)) %>%
  left_join(Ksat_geom, by = "Zone") %>%
  mutate(K_mj = Ks_cm_j / 100,
         L_q  = round(hooghoudt_L(K_mj, h = 0.5,
                                  d = D_imp, q = q_mj * q_fact), 1)) %>%
  select(Zone, q_fact, L_q) %>%
  pivot_wider(names_from = q_fact, values_from = L_q,
              names_prefix = "q_x")

kable(sens_q,
      caption = "Sensibilité de L (m) à q (+/- 50 %), critère tolérant h=0,5 m") %>%
  kable_styling(full_width = FALSE)
```

## II.5 -- Schéma indicatif du réseau de drainage

```{r drainage-schema, fig.cap="Schéma indicatif du réseau de drainage (espacements par zone)"}
# Récupère les L* du critère tolérant pour le schéma
L_use <- res %>% filter(Critere == "Tolerant (Tomate/Mais)") %>%
  select(Zone, L_iter)
print(L_use)

# Schéma simple : drains parallèles à l'axe X, espacement L par zone
ggplot() +
  # Zones (rectangles indicatifs)
  geom_rect(aes(xmin = 0,   xmax = 150, ymin = 0, ymax = 100),
            fill = "#F4D6A1", alpha = 0.5) +
  geom_rect(aes(xmin = 0,   xmax = 400, ymin = 100, ymax = 200),
            fill = "#A8C8A6", alpha = 0.5) +
  geom_rect(aes(xmin = 150, xmax = 400, ymin = 0, ymax = 100),
            fill = "#A8C8A6", alpha = 0.5) +
  geom_rect(aes(xmin = 0,   xmax = 400, ymin = 200, ymax = 250),
            fill = "#9CB5D1", alpha = 0.5) +
  annotate("text", x = 75,  y = 50,  label = "Zone A") +
  annotate("text", x = 200, y = 150, label = "Zone B") +
  annotate("text", x = 200, y = 225, label = "Zone C") +
  coord_fixed() +
  labs(title = "Schéma indicatif (à enrichir avec les drains)",
       x = "X (m)", y = "Y (m)") +
  theme_minimal()
```

> **À faire :** tracer les drains parallèles à l'axe X avec les espacements
> calculés `L_iter` (un drain tous les L mètres en Y), ajouter le collecteur
> en bordure ouest et l'exutoire.

---

# PARTIE III --- Simulation HYDRUS-1D (20 pts)

## III.1 -- Coefficient cultural Kc(t) (FAO-56)

```{r kc-fao56}
# Construction du Kc journalier par culture (4 phases)
construire_Kc <- function(culture) {
  c_row <- cult[cult$Culture == culture, ]
  Lini <- c_row$Lini; Ldev <- c_row$Ldev
  Lmid <- c_row$Lmid; Lend <- c_row$Lend
  Kini <- c_row$Kc_ini; Kmid <- c_row$Kc_mid; Kend <- c_row$Kc_fin
  cycle <- Lini + Ldev + Lmid + Lend
  t <- 1:cycle
  Kc <- numeric(cycle)
  Kc[1:Lini]                         <- Kini
  Kc[(Lini+1):(Lini+Ldev)]           <- seq(Kini, Kmid, length.out = Ldev)
  Kc[(Lini+Ldev+1):(Lini+Ldev+Lmid)] <- Kmid
  Kc[(Lini+Ldev+Lmid+1):cycle]       <- seq(Kmid, Kend, length.out = Lend)
  tibble(Jour = t, Kc = Kc, Culture = culture)
}

kc_all <- bind_rows(lapply(cult$Culture, construire_Kc))

ggplot(kc_all, aes(Jour, Kc, color = Culture)) +
  geom_line(linewidth = 1) +
  labs(title = "Coefficient cultural Kc(t) -- FAO-56",
       x = "Jour du cycle", y = "Kc (-)") +
  theme_minimal()
```

## III.2 -- Calcul de ETc(t) par culture

```{r etc}
ET0   <- clim$ET0_mm
n_max <- nrow(clim)

etc_long <- bind_rows(lapply(cult$Culture, function(c) {
  kc <- construire_Kc(c)
  kc <- head(kc, n_max)
  if (nrow(kc) < n_max) {
    kc <- bind_rows(kc, tibble(Jour = (nrow(kc)+1):n_max,
                               Kc = tail(kc$Kc, 1),
                               Culture = c))
  }
  tibble(Jour = 1:n_max, Culture = c, ET0 = ET0,
         Kc = kc$Kc, ETc = ET0 * kc$Kc)
}))

etc_long %>% group_by(Culture) %>%
  summarise(ETc_cum_mm = round(sum(ETc), 1)) %>%
  kable(caption = "ETc cumulée par culture sur 85 jours") %>%
  kable_styling(full_width = FALSE)
```

## III.3 -- Génération du fichier BC (atmospheric.in) pour HYDRUS

```{r bc-export}
# Apport hebdomadaire = ETc cumulée du lundi au dimanche, appliqué le lundi
exporter_bc <- function(culture) {
  d <- etc_long %>% filter(Culture == culture)
  d <- d %>%
    mutate(Pluie = clim$Pluie_mm,
           Semaine = (Jour - 1) %/% 7 + 1)
  besoin_sem <- d %>% group_by(Semaine) %>%
    summarise(ETc_sem = sum(ETc), Pluie_sem = sum(Pluie)) %>%
    mutate(Irr_sem = pmax(0, ETc_sem - Pluie_sem))
  d <- d %>% left_join(besoin_sem, by = "Semaine") %>%
    mutate(Irrigation = ifelse((Jour - 1) %% 7 == 0, Irr_sem, 0),
           Apport_net_mm = Pluie + Irrigation - ETc)
  fic <- sprintf("BC_HYDRUS_%s.csv", culture)
  write.csv(d %>% select(Jour, Pluie, Irrigation, ETc, Apport_net_mm),
            fic, row.names = FALSE)
  cat("Exporté :", fic, "\n")
  invisible(d)
}

for (c in cult$Culture) exporter_bc(c)
```

> **Workflow HYDRUS-1D :**
> 1. Ouvrir HYDRUS-1D et créer 12 projets (4 cultures × 3 zones).
> 2. Saisir les paramètres VG (Partie I) pour chaque zone.
> 3. Importer la chronique `BC_HYDRUS_*.csv` (Pluie + Irrigation, ET0) en
>    Time Variable Boundary Conditions.
> 4. Régler le modèle d'extraction racinaire de Feddes avec les seuils `h_3`
>    et `h_50` de la feuille `04_Cultures`.
> 5. Lancer chaque simulation, exporter les sorties (T_act.out, Balance.out,
>    Profile.out) dans `HYDRUS/<culture>_<zone>/`.

## III.4 -- Lecture des sorties HYDRUS-1D

```{r hydrus-readers, eval=FALSE}
# Cette section EST À EXÉCUTER après avoir lancé les simulations HYDRUS.
# Adapter les chemins d'accès aux dossiers HYDRUS générés.

lire_balance <- function(chemin) {
  # Fichier T_Level.out / Balance.out à parser
  # -> renvoyer un tibble (Jour, T_act, T_pot, Drainage_cum, Stock)
  ### À COMPLÉTER ###
}

resultats_hydrus <- expand.grid(Culture = cult$Culture,
                                Zone    = zones,
                                stringsAsFactors = FALSE)

# Boucle de lecture (placeholder)
resultats_hydrus <- resultats_hydrus %>%
  mutate(
    V_irr_mm = NA_real_,
    D_prof_mm = NA_real_,
    T_act_mm  = NA_real_,
    T_pot_mm  = NA_real_,
    Ta_sur_Tp = NA_real_,
    Stock_mm  = NA_real_,
    err_bilan = NA_real_
  )

kable(resultats_hydrus, caption = "Synthèse des 12 simulations HYDRUS (à remplir)")
```

---

# PARTIE IV --- Optimisation de l'assolement (30 pts)

## IV.1 -- Matrice des indicateurs 4 cultures × 3 zones

```{r matrice-indicateurs, eval=FALSE}
# Cette section nécessite les résultats HYDRUS de la Partie III.
# Pour la rédaction, remplir `resultats_hydrus` ci-dessus puis exécuter ce chunk.

# Coefficients Ky (FAO-33) à compléter par culture
Ky <- tibble(
  Culture = c("Tomate", "Pomme_terre", "Fraise", "Mais"),
  Ky      = c(1.05,     1.10,          0.85,     1.25)
)

indic <- resultats_hydrus %>%
  left_join(cult %>% select(Culture, Rdt_potentiel_t_ha,
                            Prix_DH_par_t), by = "Culture") %>%
  left_join(Ky, by = "Culture") %>%
  mutate(
    Y_rel  = pmax(0, 1 - Ky * (1 - Ta_sur_Tp)),                # rendement relatif
    Rdt    = Y_rel * Rdt_potentiel_t_ha,                       # t/ha
    P_eff  = sum(clim$Pluie_mm),                               # mm
    WP     = (Rdt * 1000) / ((V_irr_mm + P_eff) * 10),          # kg/m3
    WF     = ifelse(WP > 0, 1 / WP, NA_real_),                  # m3/kg
    eta_D  = 1 - D_prof_mm / (V_irr_mm + P_eff),
    Marge_DHha = Rdt * Prix_DH_par_t                           # DH/ha (hors coût eau)
  )

kable(indic, caption = "Matrice 12 combinaisons -- indicateurs environnementaux")
```

## IV.2 -- Les 5 scénarios d'assolement

```{r scenarios, eval=FALSE}
indic_par_zone <- indic %>% group_by(Zone) %>%
  mutate(Surface_ha = surfaces$Surface_ha[match(Zone, surfaces$Zone)])

# S0 : monoculture homogène (4 variantes)
S0 <- lapply(cult$Culture, function(c) {
  indic_par_zone %>% filter(Culture == c) %>%
    summarise(Scenario = paste0("S0_", c),
              V_irr_m3 = sum(V_irr_mm/1000 * Surface_ha * 10000),
              D_total_m3 = sum(D_prof_mm/1000 * Surface_ha * 10000),
              Marge_DH   = sum(Marge_DHha * Surface_ha),
              Prod_t     = sum(Rdt * Surface_ha))
}) %>% bind_rows()

# S1 : marge maximale par zone
S1 <- indic_par_zone %>% group_by(Zone) %>%
  slice_max(Marge_DHha, n = 1) %>% ungroup() %>%
  summarise(Scenario = "S1_marge_max",
            V_irr_m3 = sum(V_irr_mm/1000 * Surface_ha * 10000),
            D_total_m3 = sum(D_prof_mm/1000 * Surface_ha * 10000),
            Marge_DH   = sum(Marge_DHha * Surface_ha),
            Prod_t     = sum(Rdt * Surface_ha))

# S2 : volume d'eau minimum
S2 <- indic_par_zone %>% group_by(Zone) %>%
  slice_min(V_irr_mm, n = 1) %>% ungroup() %>%
  summarise(Scenario = "S2_eau_min",
            V_irr_m3 = sum(V_irr_mm/1000 * Surface_ha * 10000),
            D_total_m3 = sum(D_prof_mm/1000 * Surface_ha * 10000),
            Marge_DH   = sum(Marge_DHha * Surface_ha),
            Prod_t     = sum(Rdt * Surface_ha))

# S3 : empreinte eau minimale
S3 <- indic_par_zone %>% group_by(Zone) %>%
  slice_min(WF, n = 1) %>% ungroup() %>%
  summarise(Scenario = "S3_WF_min",
            V_irr_m3 = sum(V_irr_mm/1000 * Surface_ha * 10000),
            D_total_m3 = sum(D_prof_mm/1000 * Surface_ha * 10000),
            Marge_DH   = sum(Marge_DHha * Surface_ha),
            Prod_t     = sum(Rdt * Surface_ha))

# S4 : multicritère = drainage minimal avec Y_rel >= 0.85
S4 <- indic_par_zone %>%
  filter(Y_rel >= 0.85) %>%
  group_by(Zone) %>% slice_min(D_prof_mm, n = 1) %>% ungroup() %>%
  summarise(Scenario = "S4_multi",
            V_irr_m3 = sum(V_irr_mm/1000 * Surface_ha * 10000),
            D_total_m3 = sum(D_prof_mm/1000 * Surface_ha * 10000),
            Marge_DH   = sum(Marge_DHha * Surface_ha),
            Prod_t     = sum(Rdt * Surface_ha))

scenarios <- bind_rows(S0, S1, S2, S3, S4)
kable(scenarios, caption = "Synthèse des 5 scénarios d'assolement (10 ha)")
```

## IV.3 -- Pareto empreinte eau vs marge

```{r pareto, eval=FALSE}
scenarios <- scenarios %>%
  mutate(WF_global = V_irr_m3 / (Prod_t * 1000))   # m3/kg moyen

ggplot(scenarios, aes(WF_global, Marge_DH, label = Scenario)) +
  geom_point(size = 4, color = "#1F4E79") +
  geom_text(vjust = -0.8, size = 3) +
  labs(title = "Pareto -- empreinte eau (m3/kg) vs marge brute (DH)",
       x = "Empreinte eau globale (m3/kg)",
       y = "Marge brute totale (DH)") +
  theme_minimal()
```

---

# PARTIE V --- Analyse environnementale et économique (10 pts)

## V.1 -- Bilan de l'assolement retenu

```{r bilan-final, eval=FALSE}
scenario_retenu <- "S4_multi"   # à choisir et justifier dans le rapport

bilan <- scenarios %>% filter(Scenario == scenario_retenu)
print(bilan)

# Comparaison à la monoculture de pomme de terre (S0_Pomme_terre)
ref <- scenarios %>% filter(Scenario == "S0_Pomme_terre")

comp <- tibble(
  Indicateur = c("Volume d'eau (m3)", "Drainage total (m3)",
                 "Production (t)", "Marge brute (DH)"),
  Reference  = c(ref$V_irr_m3, ref$D_total_m3, ref$Prod_t, ref$Marge_DH),
  Retenu     = c(bilan$V_irr_m3, bilan$D_total_m3, bilan$Prod_t, bilan$Marge_DH)
) %>%
  mutate(Variation_pct = round(100 * (Retenu - Reference) / Reference, 1))

kable(comp, caption = "Comparaison de l'assolement retenu à la monoculture pomme de terre")
```

## V.2 -- Effets environnementaux indirects (à discuter dans le rapport)

> Trois effets à discuter :
>
> 1. **Lessivage d'azote** (NO3-) lié au drainage profond -- corrélation avec
>    `D_total_m3`.
> 2. **Salinisation** : un drainage insuffisant en zone C peut entraîner une
>    remontée capillaire de sels.
> 3. **Biodiversité de la rotation** : monoculture vs assolement diversifié.

## V.3 -- Recommandations chiffrées

> Formuler dans le rapport **3 recommandations concrètes** sous la forme :
>
> *Recommandation X -- Action : ... ; Cible chiffrée : ... ; Bénéfice attendu : ...*

---

# Reproductibilité

```{r session-info}
sessionInfo()
```

---

# Annexes (placeholders)

- **Annexe A** : sorties brutes des ajustements VG -- `summary(fits[["Zone_X"]])`.
- **Annexe B** : captures HYDRUS-1D (Geometry, Hydraulic Model, Root Uptake).
- **Annexe C** : tableurs des indicateurs (`resultats_hydrus.csv`).
- **Annexe D** : analyse de robustesse complète des 5 scénarios.
- **Annexe E** : déclaration anti-plagiat signée.
- **Annexe F** : répartition signée des contributions des 3 étudiants.
