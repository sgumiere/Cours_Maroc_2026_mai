---
title: "Solutions des Exemples 14.1 et 14.2"
subtitle: "Drainage en régime permanent (Hooghoudt) et transitoire (van Schilfgaarde)"
author: "Silvio Gumière — GAE-3011 Irrigation et drainage"
date: "`r format(Sys.Date(), '%d %B %Y')`"
output:
  pdf_document:
    toc: true
    toc_depth: 2
    number_sections: true
    latex_engine: pdflatex
header-includes:
  - \usepackage[utf8]{inputenc}
  - \usepackage[T1]{fontenc}
  - \usepackage[french]{babel}
  - \usepackage{amsmath, amssymb}
  - \usepackage{booktabs}
geometry: margin=2.5cm
fontsize: 11pt
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE)
options(digits = 4, scipen = 999)
```

\newpage

# Données du problème

Le champ comprend deux zones de sol. Les paramètres communs aux deux exemples sont :

| Paramètre                                | Symbole | Sol 1   | Sol 2   |
|------------------------------------------|---------|---------|---------|
| Conductivité hydraulique saturée         | $K$     | 1,00 m/j | 0,75 m/j |
| Distance drain → couche imperméable      | $d$     | 1,8 m   | 0,8 m   |
| Profondeur des drains                    | —       | 1,0 m   | 1,0 m   |
| Rayon effectif du drain (PEHD 114 mm)    | $r_e$   | 0,0151 m | 0,0151 m |

```{r donnees}
# Paramètres communs
re <- 0.0151   # rayon effectif du drain [m] (Tableau 14.3)

# Sol 1
K1 <- 1.00     # conductivité hydraulique [m/j]
d1 <- 1.80     # distance drain -> couche imperméable [m]

# Sol 2
K2 <- 0.75
d2 <- 0.80
```

\newpage

# Outil commun : profondeur équivalente $d_e$

La convergence verticale des lignes de courant près des drains conduit à
remplacer $d$ par une **profondeur équivalente** $d_e \leq d$
(van der Molen \& Wesseling, 1991) — **Éq. 14.8a** :

$$
d_e \;=\; \frac{\pi\,S}{8\!\left[\ln\!\left(\dfrac{S}{\pi\,r_e}\right) + F(x)\right]},
\qquad x = \frac{2\pi\,d}{S}
$$

avec, pour $x < 0{,}5$ (**Éq. 14.8c**) :

$$
F(x) \;=\; \frac{\pi^{2}}{4x} + \ln\!\left(\frac{x}{2\pi}\right)
$$

et, pour $x \geq 0{,}5$ (**Éq. 14.8b**) :

$$
F(x) \;=\; \sum_{i=1,3,5,\ldots} \frac{4\,e^{-2ix}}{i\,(1 - e^{-2ix})}
$$

```{r fonction-de}
# F(x) selon la valeur de x
F_x <- function(x) {
  if (x < 0.5) {
    pi^2 / (4 * x) + log(x / (2 * pi))
  } else {
    i <- seq(1, 49, by = 2)
    sum(4 * exp(-2 * i * x) / (i * (1 - exp(-2 * i * x))))
  }
}

# Profondeur équivalente d_e (Éq. 14.8a)
de_calc <- function(S, d, re) {
  x  <- 2 * pi * d / S
  Fx <- F_x(x)
  pi * S / (8 * (log(S / (pi * re)) + Fx))
}
```

\newpage

# Exemple 14.1 — Régime permanent (Hooghoudt)

## Équation de Hooghoudt

L'équation de l'ellipse, corrigée pour la convergence (**Éq. 14.9**) :

$$
\boxed{\;S \;=\; \sqrt{\dfrac{8\,K\,d_e\,b + 4\,K\,b^{2}}{R}}\;}
$$

avec $R = DC = 0{,}012$ m/j (sol minéral, grandes cultures, sans entrée
de surface — Tableau 14.2) et $b = 1{,}0 - 0{,}3 = 0{,}7$ m (la nappe ne
doit pas remonter à moins de 0,3 m de la surface).

```{r param-141}
R <- 0.012   # coefficient de drainage [m/j] (= 12 mm/j)
b <- 0.7     # hauteur de la nappe au-dessus du drain à mi-distance [m]
```

## Sol 1 — calculs étape par étape

**Itération 1** — on suppose $d_e = d = 1{,}8$ m :

```{r sol1-it1}
de <- d1
S  <- sqrt((8 * K1 * de * b + 4 * K1 * b^2) / R)
cat(sprintf("S = %.1f m\n", S))    # manuel : 31,7 m
```

**Itération 2** — on calcule $x$ puis $F(x)$ :

```{r sol1-it2}
x  <- 2 * pi * d1 / S
Fx <- F_x(x)
cat(sprintf("x = %.2f,  F(x) = %.1f\n", x, Fx))   # manuel : 0,36 ; 4,0
```

**Itération 3** — nouvelle estimation de $d_e$ :

```{r sol1-it3}
de <- pi * S / (8 * (log(S / (pi * re)) + Fx))
cat(sprintf("d_e = %.2f m\n", de))   # manuel : 1,18 m
```

**Itération 4** — on recalcule $S$ avec le nouveau $d_e$ :

```{r sol1-it4}
S <- sqrt((8 * K1 * de * b + 4 * K1 * b^2) / R)
cat(sprintf("S = %.1f m\n", S))   # manuel : 26,7 m
```

**Itérations supplémentaires** jusqu'à convergence :

```{r sol1-convergence}
for (k in 1:5) {
  de <- de_calc(S, d1, re)
  S  <- sqrt((8 * K1 * de * b + 4 * K1 * b^2) / R)
}
cat(sprintf("Convergence : S = %.1f m,  d_e = %.2f m\n", S, de))
# Manuel : S = 26,0 m, d_e = 1,11 m
```

## Sol 2 — même procédure (résolution compacte)

```{r sol2}
de <- d2
S  <- sqrt((8 * K2 * de * b + 4 * K2 * b^2) / R)
for (k in 1:10) {
  de <- de_calc(S, d2, re)
  S  <- sqrt((8 * K2 * de * b + 4 * K2 * b^2) / R)
}
cat(sprintf("Sol 2 : S = %.1f m,  d_e = %.2f m\n", S, de))
# Manuel : S = 18,3 m, d_e = 0,61 m
```

\newpage

# Exemple 14.2 — Régime transitoire (van Schilfgaarde)

## Équation de van Schilfgaarde

Pour faire passer la nappe de $b_o$ à $b$ au-dessus du drain en un temps $t$
(**Éq. 14.12**) :

$$
\boxed{\;S \;=\; \sqrt{\dfrac{9\,K\,t\,d_e}
{f\,\ln\!\left[\dfrac{b_o\,(2\,d_e + b)}{b\,(2\,d_e + b_o)}\right]}}\;}
$$

avec :

- $b_o = 1{,}0$ m (la nappe affleure la surface, drain à 1 m)
- $b = 0{,}7$ m (nappe rabaissée à 0,3 m sous la surface)
- $t = 1$ jour
- $f = 0{,}04$ m³/m³ (porosité drainable mesurée en laboratoire)

```{r param-142}
bo <- 1.0    # hauteur initiale de la nappe au-dessus du drain [m]
b  <- 0.7    # hauteur finale [m]
t  <- 1.0    # durée [j]
f  <- 0.04   # porosité drainable [m^3/m^3]
```

## Sol 1 — calculs étape par étape

**Itération 1** — on suppose $d_e = d = 1{,}8$ m :

```{r tr1-it1}
de  <- d1
num <- 9 * K1 * t * de
den <- f * log((bo * (2 * de + b)) / (b * (2 * de + bo)))
S   <- sqrt(num / den)
cat(sprintf("S = %.1f m\n", S))    # manuel : 37,4 m
```

**Itération 2** — calcul de $x$ et $F(x)$ :

```{r tr1-it2}
x  <- 2 * pi * d1 / S
Fx <- F_x(x)
cat(sprintf("x = %.3f,  F(x) = %.1f\n", x, Fx))   # manuel : 0,302 ; 5,1
```

**Itération 3** — nouveau $d_e$ :

```{r tr1-it3}
de <- pi * S / (8 * (log(S / (pi * re)) + Fx))
cat(sprintf("d_e = %.2f m\n", de))   # manuel : 1,25 m
```

**Itération 4** — nouveau $S$ :

```{r tr1-it4}
num <- 9 * K1 * t * de
den <- f * log((bo * (2 * de + b)) / (b * (2 * de + bo)))
S   <- sqrt(num / den)
cat(sprintf("S = %.1f m\n", S))   # manuel : 32,4 m
```

**Itérations supplémentaires** :

```{r tr1-convergence}
for (k in 1:5) {
  de  <- de_calc(S, d1, re)
  num <- 9 * K1 * t * de
  den <- f * log((bo * (2 * de + b)) / (b * (2 * de + bo)))
  S   <- sqrt(num / den)
}
cat(sprintf("Convergence : S = %.1f m,  d_e = %.2f m\n", S, de))
# Manuel : S = 31,8 m, d_e = 1,18 m
```

## Sol 2 — résolution compacte

```{r tr2}
de  <- d2
num <- 9 * K2 * t * de
den <- f * log((bo * (2 * de + b)) / (b * (2 * de + bo)))
S   <- sqrt(num / den)
for (k in 1:10) {
  de  <- de_calc(S, d2, re)
  num <- 9 * K2 * t * de
  den <- f * log((bo * (2 * de + b)) / (b * (2 * de + bo)))
  S   <- sqrt(num / den)
}
cat(sprintf("Sol 2 : S = %.1f m,  d_e = %.2f m\n", S, de))
# Manuel : S = 22,3 m, d_e = 0,64 m
```

\newpage

# Récapitulatif

| Sol                       | $S$ — Hooghoudt (m) | $S$ — van Schilfgaarde (m) |
|---------------------------|---------------------|-----------------------------|
| Sol 1 ($K = 1{,}00$ m/j)  | **26,0**            | **31,8**                    |
| Sol 2 ($K = 0{,}75$ m/j)  | **18,3**            | **22,3**                    |

Le régime transitoire donne un écartement plus grand parce que la
porosité drainable $f = 0{,}04$ est faible : peu d'eau doit effectivement
être déplacée pour faire chuter la nappe, donc les drains peuvent être
plus espacés.

# Références

- Huffman, R.L., Fangmeier, D.D., Elliot, W.J., \& Workman, S.R. (2013).
  *Soil and Water Conservation Engineering*, 7$^\text{e}$ éd., ASABE, ch. 14.
- van der Molen, W.H., \& Wesseling, J. (1991). *Agric. Water Manage.*, 19 : 1–16.
- van Schilfgaarde, J. (1963). *Am. Soc. Civil Eng. Proc.*, 89(IR2) : 1–13.
